import 'package:flutter/material.dart';

class ListQuizBattle extends StatelessWidget {
  const ListQuizBattle({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}